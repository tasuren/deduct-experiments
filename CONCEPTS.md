# `deduct_poc` Concepts

## このプロジェクトは何か

`deduct_poc` は、物事の筋道や議論の進み方を、明示的なルールと状態遷移として扱うための実験的な演繹システムです。

これは一般的な意味での「定理証明器」を目指したものではありません。  
むしろ、

- ある前提がそろっているときに
- ある出来事や推論を適用すると
- 状態が次にどう変わるか

を検証するための小さな実行系です。

## 基本的な考え方

このプロジェクトの中心にある考え方は、**論理を静的な真理の集合ではなく、動的なプロセスとして扱う**ことです。

ふつうの論理の説明では、「A ならば B」という関係を一度認めると、その知識はずっと残り続けます。  
一方 `deduct_poc` では、ある推論や行為を実行すると、もとの前提が消費され、次の状態へ移ることがあります。

そのためこのシステムは、

- 命題を積み上げていく知識ベース
- 数学的定理の証明支援

というより、

- 議論の進行
- 状況の変化
- 所有や役割の移動
- 「その一手を今ここで打てるか」の検証

に向いた発想を持っています。

## 何を表現しているのか

このシステムが表現しているのは、主に次の 2 つです。

1. 一般化された遷移パターン
2. その具体的な実行

前者は「どういう条件がそろっていれば、どういう結果になるか」という一般論です。  
後者は「この具体的な人物、この具体的な対象について、その一般論を適用するとどうなるか」という個別事例です。

この分離により、議論や文章の破綻を見たときに、

- 一般ルール自体がまずいのか
- 個別の適用がまずいのか

を分けて考えられます。

## このシステムの意味論

`deduct_poc` の意味論を一言で言うと、**前提を消費しながら結論へ進む前方実行**です。

ここで重要なのは次の点です。

- 推論は前から順に実行される
- 実行のたびに現在の状態が更新される
- 必要な前提がなければ、その推論は成立しない
- 使われた前提は、そのまま残るとは限らない

つまり「何が真か」だけでなく、「何が今使える資源として残っているか」が重要になります。

この性質のため、`deduct_poc` は古典的な単調論理よりも、リソース感覚を持つ論理や状態機械に近い振る舞いをします。

## なぜ前提を消費するのか

この設計の特徴は、前提が単なる条件ではなく、**状態の中に存在する資源**として扱われることです。

これは例えば次のような状況と相性が良いです。

- ある物を持っている人が、それを他人へ渡す
- あるフェーズを通過すると、以前のフェーズには戻らない
- 議論が進み、古い前提が「処理済み」になる
- ある条件を満たしたので、次の段階へ遷移する

このような文脈では、「前提は使ったあとも永遠に残る」と考えるより、「前提を消費して新しい状態へ移る」と考える方が自然です。

## どういう問いに向いているか

このプロジェクトは、次のような問いに向いています。

- この筋書きは、前提不足なく進められているか
- この一手は、現在の状態から本当に実行可能か
- 議論の途中で、暗黙の前提に依存していないか
- 状態の移り変わりを、明示的な規則として表現できるか

逆に、次の用途にそのまま向いているわけではありません。

- 高度な数学証明の構築
- 豊富な論理記号体系の操作
- 完全な単一化アルゴリズムを使う論理プログラミング
- 矛盾解消や非決定的探索を備えた大規模推論

現状の `deduct_poc` は、そうした汎用論理エンジンではなく、考え方を検証するための最小構成の PoC です。

## 文章や思考の検証との関係

このプロジェクトが面白いのは、文章や思考の検証にも読み替えやすいことです。

文章にはしばしば、書き手にとっては自明でも、読み手には共有されていない前提があります。  
`deduct_poc` の考え方では、そのような前提も明示的に状態に存在していなければなりません。

そのため、ある主張や展開が成立するために必要な前提を洗い出す補助になります。  
言い換えると、これは「正しい答えを自動生成する道具」というより、**論理の飛躍をあぶり出す道具**として理解する方が適切です。

## 中心となる概念

このプロジェクトでは、概念上おおむね次の対応で理解できます。

- `Entity`: 登場人物、対象物、具体的な概念
- `Generic`: 一般ルールの中で使う変数
- `Rule`: 一般化された遷移ルール
- `Effect`: 具体的に起きた、あるいは適用しようとしている事象
- `StateMachine`: 事象列を順に適用し、状態を更新する実行器

このとき最も重要なのは `Effect` です。  
`Effect` は単なる「結果」ではなく、システムの中で実際に処理される単位です。

## このプロジェクトをどう捉えると理解しやすいか

初めて読む人にとっては、これを次のいずれとして見るかで理解のしやすさが変わります。

- 論理証明器
- ルールエンジン
- ステートマシン
- 思考のデバッガー

一番近いのは、**状態遷移を扱うルールエンジンであり、思考や議論の筋道を点検するための最小実装**という捉え方です。

## 現時点での割り切り

このプロジェクトは PoC なので、設計上かなり割り切っています。

- 表現は小さい
- 単一化は限定的
- 状態探索は単純
- 矛盾管理はまだない
- 根拠追跡もまだない

しかしその分、中心となるアイデアは見えやすくなっています。  
つまりこのプロジェクトの価値は、「完成された推論基盤」であることより、**どのような意味論で演繹を扱いたいかを明示していること**にあります。

## 要約

`deduct_poc` は、

- ルールを使って
- 具体的な出来事を順に適用し
- 前提を消費しながら
- 次の状態へ進む

という考え方で設計された実験的な演繹システムです。

これを理解するときは、「知識を蓄える論理」よりも、「状態を進める論理」として捉えるのが最も自然です。
