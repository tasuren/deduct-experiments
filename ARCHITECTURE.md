# `deduct_poc` Architecture

## 概要

`deduct_poc` は、ルールに基づいて状態を前進させる小さな演繹エンジンです。  
普通の「真偽を蓄積する論理系」というより、**状態の中にある事実を消費しながら次の事実へ進む遷移系**として読むと理解しやすい構造になっています。

このパッケージの中心は次の 3 層です。

1. `Rule`: どういう前提があると、どんな結論へ進めるかを定義する雛形
2. `Effect`: ある `Rule` に具体的な対象を割り当てた実例
3. `StateMachine`: `Effect` の列を順番に処理し、状態を更新する実行器

`samples/dead.py` の例で言えば、「ソクラテスは人間である」という事実と「人間なら死ぬ」というルールを使って、状態を進行させます。

## 設計の全体像

### 1. `Entity`: 固定された具体物

[`Entity`](./src/deduct_poc/entity.py) は単なる名前付きデータです。

- 例: `Entity("ソクラテス")`
- 役割: 具体的な人物、物、概念などを表す

実装は `name: str` だけを持つ `dataclass` です。  
この層には推論ロジックはありません。

### 2. `Generic`: ルールの引数スロット

[`Generic`](./src/deduct_poc/rule.py) は「ここには何らかの `Entity` が入る」という穴です。

- 例: `Generic("Die S")`
- 役割: ルール定義時の変数

`Rule.args` や `Effect.associated` のキーとして使われます。  
設計上は「同じ名前の変数」よりも、**同じ `Generic` インスタンスを使い回す**ことが前提です。

### 3. `Rule`: 遷移の雛形

[`Rule`](./src/deduct_poc/rule.py) は 4 つの要素から成ります。

- `name`: ルール名
- `args`: そのルールが受け取る引数
- `premise`: このルールを成立させるのに必要な前提 `Effect`
- `conclusion`: 成立後に状態へ追加する結論 `Effect`

重要なのは、`premise` と `conclusion` もまた `Effect` である点です。  
つまりこのシステムでは、「あるルールの実行条件」と「実行結果」の両方を、同じ `Effect` という表現で統一しています。

### 4. `Effect`: ルールの具体化

[`Effect`](./src/deduct_poc/effect.py) は、`Rule` に対して `Generic -> Entity` または `Generic -> Generic` の対応を与えたものです。

- `rule`: どのルールの実例か
- `associated`: ルール引数への割り当て

例えば `samples/dead.py` では:

```python
Effect(s_is_human, {is_s: socrates})
```

は「`人間である(Is S, 人間)` というルールに対して、`Is S = ソクラテス` を入れた具体例」です。

### 5. `StateMachine`: 実行器

[`StateMachine`](./src/deduct_poc/state_machine.py) は次の 2 つを持ちます。

- `these`: これから順に処理する `Effect` のキュー
- `state`: 現在成立している `Effect` の一覧

`these` は入力列、`state` はワーキングメモリです。  
`proceed()` は `these` を先頭から 1 つずつ処理し、最終的な `state` を返します。

## 実行意味論

このライブラリの意味論は [`StateMachine.proceed_once()`](./src/deduct_poc/state_machine.py) に集約されています。  
1 ステップの流れは以下です。

1. `these` の先頭 `Effect` を取り出す
2. その `Effect` が要求する `premise` を、現在の引数束縛で具体化する
3. 具体化された各前提が `state` に存在するか確認する
4. 見つかった前提は `state` から削除する
5. `conclusion` を同じ束縛で具体化して `state` に追加する
6. `conclusion` が空なら、その `Effect` 自身を `state` に追加する

ここで大事なのは 3 点です。

- 前方推論である  
  `these` に書かれた出来事を前から実行し、その結果として状態が変わる
- 前提は消費される  
  満たした前提は `state` から削除される
- 結論が空のルールは「状態に残る事実」を表す  
  ルール自体の `Effect` がそのまま `state` に積まれる

このため、一般的な単調論理というより、**リソース消費を伴う状態遷移**に近い挙動になります。

## 束縛の仕組み

### `hydrate()`

[`Effect.hydrate()`](./src/deduct_poc/effect.py) は、ある `Effect` 内に含まれる `Generic` を、外側の `Effect` の割り当てで埋めます。

例えば次の前提テンプレートがあるとします。

```python
Effect(s_is_human, {is_s: die_s})
```

これは「`die` ルールの `die_s` が、`s_is_human` ルールの `is_s` に入る」という意味です。  
ここに `die_s = ソクラテス` が渡されると、前提は実質的に次の形へ具体化されます。

```python
Effect(s_is_human, {is_s: socrates})
```

### `is_bounded()`

[`Effect.is_bounded()`](./src/deduct_poc/effect.py) は、前提テンプレートが現在の状態中の `Effect` によって満たされるかを調べます。

判定条件は以下です。

- `rule` が同じ
- 前提側で `Entity` が指定されている位置は完全一致
- 前提側で `Generic` が残っている位置はワイルドカードとして扱う

つまりこれは厳密な単一化ではなく、**「具体値が書かれている部分だけを比較する部分一致」**です。

## サンプルで見る状態遷移

### `dead.py`

[`samples/dead.py`](./samples/dead.py) では次の 2 つの `Effect` を順に処理します。

1. `Effect(s_is_human, {is_s: socrates})`
2. `Effect(die, {die_s: socrates})`

`s_is_human` は `conclusion` が空なので、この 1 つ目を処理すると状態にそのまま追加されます。

次に `die` を処理すると:

- 前提 `Effect(s_is_human, {is_s: die_s})` を具体化する
- `die_s = socrates` が埋まり、`Effect(s_is_human, {is_s: socrates})` になる
- それが状態にあるので消費する
- `die` 自体も `conclusion` が空なので、`Effect(die, {die_s: socrates})` が状態に残る

結果として、「ソクラテスが人間である」という前提は消え、「ソクラテスが死ぬ」が最終状態になります。

### `having.py`

[`samples/having.py`](./samples/having.py) では「髙木が猫を持っている」という状態があり、その後に「髙木が魔理沙へ猫を渡す」を実行します。

`give` ルールの意味は次です。

- 前提: `X` が `O` を持っている
- 結論: `Y` が `O` を持っている

したがって状態遷移としては:

1. `髙木が猫を持つ` が状態に入る
2. `髙木が魔理沙へ猫を渡す` を処理する
3. 前提の `髙木が猫を持つ` を消費する
4. 結論として `魔理沙が猫を持つ` を状態へ追加する

この例も、「事実の蓄積」ではなく「所有状態の移動」として読むのが適切です。

## エラー設計

[`error.py`](./src/deduct_poc/error.py) では 2 種類の失敗を区別しています。

### `NoPremiseBoundingError`

必要な前提が現在の `state` に見つからなかったときのエラーです。

これは「いまこの手順を実行するだけの前提条件が足りない」ことを意味します。  
文章の論理チェックという観点では、論理の飛躍や前提不足に相当します。

### `ValidationError`

前提または結論の具体化中に `ValueError` が起きたときのラッパーです。  
要するに、`Rule` と `Effect` の組み合わせ自体が壊れているときのエラーです。

## この設計が表しているもの

このパッケージは、「命題の真偽」を扱うというより、**ある出来事が起きると利用可能な状態がどう移り変わるか**を扱っています。

そのため、各クラスは次のように読むと整理しやすいです。

- `Entity`: 登場人物や対象物
- `Generic`: ルール内の変数
- `Rule`: 一般化された遷移パターン
- `Effect`: 実際に起きた、または起こそうとしている出来事
- `StateMachine`: 出来事列を順に適用して状態を更新する機械

この見方を取ると、`deduct_poc` は「論理証明器」というより、**演繹可能性をチェックするための小さな実行系**です。

## 実装上の注意点

初見の人が読んでおくとよい注意点がいくつかあります。

### `Generic` は実質的にインスタンス同一性へ依存している

`Generic` は `name` を持ちますが、辞書キーとして使われるため、実際には同じ意味の変数でも**同じインスタンスを共有する前提**で書かれています。  
別インスタンスを同名で作ると、見た目は同じでも正しく対応付けできない可能性があります。

### `Effect.copy()` は浅いコピー

`Effect.copy()` は `copy.copy()` を使う浅いコピーです。  
そのため `associated` 辞書は共有されたままで、`hydrate()` はその辞書を破壊的に書き換えます。

現在の実装では、`Rule.premise` や `Rule.conclusion` に置いたテンプレート `Effect` が、実行中の具体化によって変化しうる点に注意が必要です。  
設計意図としてはテンプレートを都度複製して使いたいように見えますが、現状のコードはそこまで防御していません。

### 状態はリストで、多重集合的に扱われる

`state` は `list[Effect]` です。  
同じ `Effect` が複数回入ることを禁止しておらず、前提探索でも最初に見つかった 1 件だけが消費されます。

したがって意味論としては集合ではなく、**順序付きの多重集合に近い**実装です。

## まとめ

`deduct_poc` の中核アイデアは単純です。

- ルールはテンプレートとして定義する
- 具体的な出来事は `Effect` として並べる
- `StateMachine` がそれらを順に実行する
- 前提を消費し、結論へ状態を移す

初見では `Rule` と `Effect` の入れ子が少し抽象的ですが、  
「`Rule` は型、`Effect` はそのインスタンス」「`state` はいま成立している資源」と捉えると、全体の設計は一貫しています。
