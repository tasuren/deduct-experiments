# `deduct_poc` Overview

## 概要

`deduct_poc` は、ルールに基づいて状態を更新する実験的な Python プログラムです。  
入力として「一般ルール」と「具体的な出来事の列」を与え、それらを順に処理して最終状態を得ます。

このプログラムの特徴は、推論結果をただ追加していくのではなく、**必要な前提を現在の状態から探し、見つけたらそれを消費して次の状態へ移る**ことです。

## プログラムの役割

このプログラムは、大きく言えば次のことをします。

1. ある事象を実行するのに必要な前提を確認する
2. 前提が満たされていれば状態を進める
3. 満たされていなければエラーにする

そのため、これは単純なデータ保存ではなく、**手続きの正当性を検証しながら状態を遷移させる仕組み**です。

## 入力として必要なもの

このプログラムを使うには、概念的には次の 3 種類を与えます。

### 1. 登場する具体物

人、物、概念などの具体的な対象です。  
例としては「ソクラテス」「猫」「魔理沙」などです。

### 2. 一般ルール

「どういう条件なら、どういう結果になるか」を表すルールです。

例えば、

- 人間なら死ぬ
- 持っている物を渡したら、相手がそれを持つ

のような形です。

### 3. 実際に処理したい出来事の列

一般ルールだけでは何も起きません。  
そのルールを、どの対象に対して、どの順番で適用するかを並べた列が必要です。

この列が、プログラムにとっての実行入力です。

## 出力

出力は、すべての入力を順に処理したあとの最終状態です。  
ここでいう状態とは、「いま成立している `Effect` の一覧」です。

もし処理の途中で必要な前提が見つからなければ、正常終了せずエラーになります。

## 内部の主要コンポーネント

プログラム全体は、少数の型で構成されています。

### `Entity`

具体的な対象を表します。  
人、物、概念などの「中身の入った値」です。

### `Generic`

ルールの中で使う変数です。  
「誰か」「何か」に相当する抽象スロットです。

### `Rule`

一般化された遷移パターンです。  
そのルールがどんな引数を持つか、どんな前提を要求するか、どんな結論を生むかを持ちます。

### `Effect`

ある `Rule` に具体的な対象を結びつけたものです。  
実際に処理される単位であり、前提にも結論にも入力列の要素にも使われます。

### `StateMachine`

`Effect` の列を順に処理する実行器です。  
現在状態と未処理キューを持ち、1 ステップずつ進めます。

## 処理の流れ

1 つの出来事を処理するとき、プログラムは概ね次の順で動きます。

1. 処理対象の `Effect` を取り出す
2. その `Effect` が必要とする前提を、現在の具体値で埋める
3. その前提が現在状態にあるか探す
4. 見つかればその前提を消費する
5. 結論を生成して状態へ追加する
6. 結論がないルールなら、その `Effect` 自体を状態へ残す

この処理を入力列の最後まで繰り返します。

## 典型的な使い方

### 例 1: ソクラテスは死ぬ

考え方としては次の流れです。

- まず「ソクラテスは人間である」という状態を入れる
- 次に「人間なら死ぬ」というルールをソクラテスへ適用する
- プログラムは「ソクラテスは人間である」が前提として存在するか確認する
- 存在するので、それを消費して「ソクラテスは死ぬ」を状態に残す

この例では、単に結論を追加するだけではなく、前提から次の状態へ遷移している点が重要です。

### 例 2: 物を渡す

別の例として、所有の移動を表現できます。

- 最初の状態は「髙木が猫を持っている」
- 次の出来事は「髙木が魔理沙に猫を渡す」
- プログラムは、髙木が猫を持っていることを前提として確認する
- 確認できたらその状態を消費する
- 結果として「魔理沙が猫を持っている」を追加する

この場合、状態は蓄積ではなく移動として振る舞います。

## このプログラムの見方

このプログラムは、次のように理解すると分かりやすいです。

- データベースではない
- 証明木を構築する証明器でもない
- 決定木探索のような大規模推論器でもない
- 状態付きの小さなルール実行器である

つまり、「ルールにしたがって出来事を適用すると、状態がどう変わるか」を確かめるための最小構成のエンジンです。

## どこが未成熟か

PoC なので、現時点ではかなり単純化されています。

- 変数束縛は限定的
- 探索戦略は単純
- 矛盾検知はない
- 根拠追跡はない
- ドメイン表現も最小限

したがって、完成度の高い汎用推論基盤として使うというより、**どういう設計で演繹や状態遷移を扱うかを試すための土台**として見るのが適切です。

## 短い要約

`deduct_poc` は、前提を消費しながら結論へ進む状態遷移型の小さな演繹エンジンであり、議論や手続きの筋道が成立しているかを検証するための PoC である。
